# Express API + legacy static HTML (pages/, styles/, scripts/) during incremental migration.
# When set, Next.js dev server proxies these paths to the backend (see web/next.config.mjs).
# Example: BACKEND_ORIGIN=http://127.0.0.1:5000
BACKEND_ORIGIN=
